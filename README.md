## 🚀 Запуск проекта

Для запуска требуется установленный **Docker** и **Docker Compose**.

```bash
docker compose build
docker compose up -d
```
(у меня он запускался пол часа🫡🫡🫡🫡)
---

## 🌐 Доступные интерфейсы

* **Frontend UI**:
  `http://localhost:7000/app/`
* **API Gateway Scalar (OpenAPI)**:
  `http://localhost:7000/scalar/`

---

Ниже текст “от моего лица”, коротко и по делу.

---

## Transactional Outbox — надёжная публикация событий

Я публикую события не напрямую в RabbitMQ, а через **таблицу Outbox в базе сервиса**.

Как это работает:

* Когда я выполняю бизнес-операцию (например, создаю заказ), я **в одной транзакции**:

  1. сохраняю изменения доменных данных (например, запись заказа),
  2. добавляю запись в **Outbox** (например, `OutboxPaymentRequested`) со всеми нужными полями события.
* Отдельный фоновый процесс/воркер регулярно читает Outbox, где `PublishedAt IS NULL`, и публикует события в RabbitMQ.
* После успешной публикации я помечаю запись как отправленную (`PublishedAt = now`).

Зачем: если сервис упадёт после коммита БД — событие **не потеряется**, оно будет отправлено при следующем запуске.

---

## Transactional Inbox — защита от повторной обработки сообщений

На входящих сообщениях я использую **Inbox-таблицу** (или аналогичный механизм), чтобы сообщение с одинаковым `MessageId` обрабатывалось **только один раз**.

Как это работает:

* Каждое входящее событие содержит уникальный `MessageId`.
* Перед обработкой я проверяю, нет ли этого `MessageId` в Inbox.

  * если есть → значит сообщение уже обработано → я выхожу без повторных действий,
  * если нет → я записываю `MessageId` в Inbox и обрабатываю событие.
* Запись в Inbox и бизнес-изменения я делаю **в одной транзакции**.

---

## Идемпотентная логика — защита от повторного списания средств

Даже если событие “списать деньги” придёт повторно, списание не должно повториться. Для этого я делаю обработчик **идемпотентным**.

Как это реализовано:

* Ключ идемпотентности — `OrderId` или `MessageId` (в зависимости от события).
* Перед списанием я проверяю, не было ли уже обработано списание по этому ключу:

  * через Inbox,
  * и/или через отдельное поле/таблицу “результат обработки”.
* Если списание уже выполнено — я возвращаю “успех” без повторной операции.

---

## Семантика: effectively exactly once

“Идеально ровно один раз” в распределённых системах недостижимо на 100%, поэтому я делаю **effectively exactly once**:

* Сообщение может прийти несколько раз,
* но **эффект в данных** будет как будто оно пришло один раз.

Это достигается сочетанием:

* Outbox (не теряем события),
* Inbox (не обрабатываем дубли),
* идемпотентности (не повторяем критичные действия).

---

## RabbitMQ — обмен событиями между сервисами

RabbitMQ у меня — это брокер сообщений для связи сервисов по событиям.

Как я использую:

* Orders публикует событие типа “нужна оплата” (через Outbox → RabbitMQ).
* Payments подписан на это событие, обрабатывает его и публикует результат оплаты (так же через свой Outbox).
* Orders слушает “результат оплаты” и обновляет статус заказа.

---

## Nginx Gateway — единая точка входа и проксирование REST

Я использую Nginx как **API Gateway**:

* Все внешние запросы идут в одну точку: `http://localhost:7000`
* Nginx по префиксу маршрута проксирует запросы в нужный сервис:

  * `/orders` → Orders API
  * `/accounts` → Accounts/Payments API
  * `/app` → фронт
  * `/scalar` и `/.../openapi.json` → документация

---

## Scalar — просмотр OpenAPI спецификаций

Scalar у меня нужен для удобного просмотра OpenAPI:

* Nginx отдаёт `.../openapi.json` каждого сервиса,
* Scalar подхватывает эти спецификации и показывает документацию в браузере.

---

<div align="center">
  <img src="https://media1.tenor.com/m/FSg_bcfCjeUAAAAC/gif-meme.gif" width="420" />
</div>




